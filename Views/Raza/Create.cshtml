@model Turnos31.Models.Raza

@{
    ViewData["Title"] = "Nueva Raza";
}

<div class="row">
    <div class="col s12">
        <div class="card">
            <div class="card-content">
                <span class="card-title">Nueva Raza</span>
                <form asp-action="Create" method="post">
                    <div class="row">
                        <div asp-validation-summary="All" class="red-text"></div>
                        <div class="input-field col s12">
                            <i class="material-icons prefix">pets</i>
                            <input asp-for="Nombre" class="validate" required />
                            <label asp-for="Nombre"></label>
                            <span asp-validation-for="Nombre" class="red-text"></span>
                        </div>
                        <div class="input-field col s12">
                            <i class="material-icons prefix">category</i>
                            <select asp-for="IdEspecie" asp-items="ViewBag.IdEspecie" class="validate" required>
                                <option value="">Seleccione una especie</option>
                            </select>
                            <label>Especie</label>
                            <span asp-validation-for="IdEspecie" class="red-text"></span>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col s12">
                            <button class="btn waves-effect waves-light green lighten-1" type="submit">
                                <i class="material-icons left">save</i>
                                Guardar
                            </button>
                            <a class="waves-effect waves-light btn grey" asp-action="Index">
                                <i class="material-icons left">arrow_back</i>
                                Volver
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        $(document).ready(function(){
            $('select').formSelect();
        });
    </script>
} 